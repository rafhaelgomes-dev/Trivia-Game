import React, { Component } from 'react';
import Header from '../Components/Header';

class Game extends Component {
  render() {
    return (
      <section>
        <div>Game</div>
        <Header />
      </section>
    );
  }
}

export default Game;
